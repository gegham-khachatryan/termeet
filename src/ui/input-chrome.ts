/** OpenTUI input `style` — text only so the field blends with the parent (no fill). */
export const inputChrome = {
  textColor: "#e8eef8",
  placeholderColor: "#5a6a7a",
  focusedTextColor: "#ffffff",
} as const
